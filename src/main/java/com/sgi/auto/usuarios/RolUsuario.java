package com.sgi.auto.usuarios;

public enum RolUsuario {
    DUENO,      // Dueño / administrador — acceso total
    CAJERA,     // Cajera — acceso al POS, caja y clientes
    MECANICO    // Mecánico — acceso al taller
}